language: c
arch:
  - amd64
  - arm64 
os: linux 

compiler:
  - gcc

before_script:
  - sudo apt install build-essential -y
  - cd C-edition
  - mkdir build/
  - cd build
  - cmake -DCMAKE_BUILD_TYPE=Release ..

script:
  - make
  - sudo make install
